[package]
name = "parley"
version = "0.4.0"
edition = "2021"
description = "Parley — one prompt interface for every AI coding agent: route, resume, bridge, and converse across them."
license = "MIT"

[[bin]]
name = "par"
path = "src/main.rs"

[profile.release]
codegen-units = 1
lto = "thin"
strip = true
