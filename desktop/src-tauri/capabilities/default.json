{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Parley desktop window: core events + window controls.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
